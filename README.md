Decentralized voting Dapp with admin GUI, ERC20 voter rewards, Merkle Tree-based voter book, and anonymous candidate matching based on voter preferences. Includes timed elections, candidate ranking, and a smart contract backend.

## How to Run
npm install
npm install ethers
npx hardhat compile
npx hardhat run scripts/deploy.js --network sepolia
node index.js 
